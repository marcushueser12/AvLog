import React from 'react';

interface LandscapePromptProps {
  show: boolean;
}

/** No longer shown: mobile is fully supported. Kept for API compatibility. */
const LandscapePrompt: React.FC<LandscapePromptProps> = () => {
  return null;
};

export default LandscapePrompt;
